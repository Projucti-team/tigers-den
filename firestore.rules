rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The Roar — public read, server-only writes (via Firebase Admin on POST /api/match-chat).
    match /roar_messages/{messageId} {
      allow read: if true;
      allow create, update, delete: if false;
    }
  }
}
